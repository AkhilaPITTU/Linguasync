# Python 3.11 / Render runtime dependencies for LINGUASYNC.
# Translation uses Google Translate's free web endpoint (via deep-translator,
# no API key); no local model is retained. Previously used the public
# MyMemory REST API, replaced because MyMemory's free tier aggressively
# rate-limits/blocks shared cloud IPs (such as Render's), which silently
# returned untranslated text as a false "success".

# Web application and persistence
fastapi==0.140.0
uvicorn[standard]==0.51.0
motor==3.7.1
pymongo==4.17.0
python-dotenv==1.2.2
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
email-validator==2.3.0
python-multipart==0.0.32
fastapi-mail==1.6.5

# Audio and speech recognition
numpy==2.4.6
av==18.0.0

# Translation API client
requests==2.34.2
deep-translator==1.11.4

# Existing application features
edge-tts==7.2.8

# Conversation PDF export: rendering + real cryptographic PDF signing
# (PAdES / PKCS#7, not a watermark or checksum). cryptography is already
# pulled in by python-jose[cryptography] above; pinned explicitly here
# since this feature depends on it directly.
reportlab>=4.2
cryptography>=42
pyhanko==0.37.0
pyhanko-certvalidator>=0.32
asn1crypto>=1.5
arabic-reshaper>=3.0
python-bidi>=0.6
